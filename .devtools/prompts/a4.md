# Task

Consider yourself a Senior Go developer.
I would like to complete the `zshchso.go` file. Other files (only the important ones) are provided below.
Features to implement:
- `GetAvailableOsType` must return only the Linux type.
- `IsInstalled` checks the user's terminal. If it is zsh, then it is considered installed.
- No parameters, no GetBeforeIDs, and no features.
- `ExecInstall` performs an action similar to `chsh -s $(which zsh)`. You can use `platform.ExecCommand`.

Your output MUST use the following format: an h2 markdown heading for the file name (the file path to be created), and the markdown code block containing the file content.
